---
interface Props {
  text?: string;
  type?: string;
  class?:string;
  id?:string;
}
const { text,type,class:className,id } = Astro.props;
---
<a href="#" id={id} class={`btn ${className ? className:''}`} data-type={type}>
  <slot name="icon-left"/>
  {text}
  <slot name="icon-right"/>
</a>